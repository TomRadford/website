---
import { Image } from 'astro:assets'
import type { ImageMetadata } from 'astro'

interface Props {
  alt: string
  caption: string
  decorative?: boolean
  src: ImageMetadata
}

const { alt, caption, decorative = false, src }: Props = Astro.props
---

<article class="w-[18.75rem] flex-none max-md:w-[15rem] max-md:snap-start">
  <Image
    alt={decorative ? '' : alt}
    class="h-56 w-full rounded-xl object-cover max-md:h-[11.25rem]"
    loading="lazy"
    src={src}
  />
  <p class="mt-2 text-xs text-muted-foreground">{caption}</p>
</article>
