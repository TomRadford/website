---
interface Props {
  labels: string[]
}

const { labels } = Astro.props
---

<div class="flex shrink-0 items-center gap-6 pr-6">
  {
    labels.map((label) => (
      <span class="text-2xl whitespace-nowrap text-center font-black leading-none text-brand-text md:text-3xl">
        {label}
      </span>
    ))
  }
</div>
